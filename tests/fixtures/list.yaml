items: [1, 2, 3]
fallback_items: [null, null, found]

padded_right: ${rpad:${items},0,5}
padded_left: ${lpad:${items},0,5}
n_items: ${len:${items}}
first_non_null: ${coalesce:${fallback_items}}
epochs: ${range:0,6,2}
